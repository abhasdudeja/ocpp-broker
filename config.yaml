broker:
  host: 0.0.0.0
  port: 8765

organizations:
  - name: orgA
    connect_to_backend: true   # ✅ Will connect to external backends
    backends:
      - id: backendA1
        url: ws://localhost:90001/EV/OCPPBACKEND
        leader: false
        chargers:
          - SLTEST01
          - CHG002

  - name: orgB
    connect_to_backend: false  # 🎯 Broker acts as backend (new feature)
    backends: []  # No external backends needed
    chargers:
      - BCG001
