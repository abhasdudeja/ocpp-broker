broker:
  host: 0.0.0.0
  port: 8765

organizations:
  - name: orgA
    connect_to_backend: true   # ✅ Will connect to backends
    backends:
      - id: backendA1
        url: ws://ocppmgl.ev-opt.com/EV/EDMS
        leader: true
        chargers:
          - SLTEST01
          - CHG002

  - name: orgB
    connect_to_backend: false  # 🚫 Skips backend connections
    backends:
      - id: backendB1
        url: ws://localhost:9002
        chargers:
          - BCG001
